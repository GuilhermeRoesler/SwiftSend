---
description: Build/run SwiftSend — aponta para a skill de scripts e empacotamento
globs: python/**/*,csharp/**/*,installer/**/*,run.bat,run.sh,.github/workflows/release.yml
alwaysApply: false
---

# SwiftSend — Build e Run

Ao alterar scripts de execução, PyInstaller, csproj, publish ou instaladores, leia:

`.cursor/skills/swiftsend-build-run/SKILL.md`

Raiz `run.*` → Python; release = instaladores Python (Win/Linux/macOS); C# publish é só local (`csharp/publish.bat`).
