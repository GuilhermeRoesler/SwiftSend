---
description: Build/run SwiftSend — aponta para a skill de scripts e empacotamento
globs: python/**/*,csharp/**/*,run.bat,run.sh
alwaysApply: false
---

# SwiftSend — Build e Run

Ao alterar scripts de execução, PyInstaller, csproj ou publish, leia:

`.cursor/skills/swiftsend-build-run/SKILL.md`

Raiz `run.*` → Python; C# é Windows (`dotnet publish` single-file).
